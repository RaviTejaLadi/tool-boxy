import ImageAnnotator from '@/features/ImageAnnotator';

const ImageAnnotatorPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <ImageAnnotator />
    </div>
  );
};

export default ImageAnnotatorPage;
