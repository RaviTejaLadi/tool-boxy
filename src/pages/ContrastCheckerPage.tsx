import ContrastChecker from '@/features/ContrastChecker';

const ContrastCheckerPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <ContrastChecker />
    </div>
  );
};

export default ContrastCheckerPage;
