import ImageFilters from '@/features/ImageFilters';

const ImageFiltersPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <ImageFilters />
    </div>
  );
};

export default ImageFiltersPage;
