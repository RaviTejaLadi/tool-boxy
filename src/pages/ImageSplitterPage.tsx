import ImageSplitter from '@/features/ImageSplitter';

const ImageSplitterPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <ImageSplitter />
    </div>
  );
};

export default ImageSplitterPage;
