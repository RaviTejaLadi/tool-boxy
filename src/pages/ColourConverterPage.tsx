import ColourConverter from '@/features/ColourConverter';

const ColourConverterPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <ColourConverter />
    </div>
  );
};

export default ColourConverterPage;
