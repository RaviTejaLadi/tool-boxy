import Base64ImageEncoder from '@/features/Base64ImageEncoder';

const Base64ImageEncoderPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <Base64ImageEncoder />
    </div>
  );
};

export default Base64ImageEncoderPage;
