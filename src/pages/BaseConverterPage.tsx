import BaseConverter from '@/features/BaseConverter';

const BaseConverterPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <BaseConverter />
    </div>
  );
};

export default BaseConverterPage;
