import GlyphBrowser from '@/features/GlyphBrowser';

const GlyphBrowserPage = () => {
  return (
    <div className="flex h-full min-h-0 flex-1 flex-col">
      <GlyphBrowser />
    </div>
  );
};

export default GlyphBrowserPage;
